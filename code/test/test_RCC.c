#include "unity.h"

void test_RCC(void);
void setUp(void)
{
}

/// @brief simple_Test
/// @param
void test_RCC(void)
{
    struct PLL_mull expected;
    uint8_t HSE = 8;
    uint8_t need = 120;
    TEST_ASSERT_EQUAL_INT32(expected)
    struct PLL_mulls *calculatePLL(uint8_t HSE, uint8_t need);
}